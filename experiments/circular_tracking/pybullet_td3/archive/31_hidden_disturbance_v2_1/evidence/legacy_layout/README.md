# Protocol v2.1

This directory mirrors the local protocol-v2.1 result layout. GitHub retains
compact gate summaries and run metadata, while large training outputs remain
local under the repository exclusion policy. This README is a layout marker,
not a model or checkpoint.
