# Protocol v2.1 — Gate 2 training

The local tree contains per-controller and per-seed training directories here.
GitHub keeps only compact metadata and completion markers; checkpoints, replay
buffers, caches, and trainer logs remain local. This README preserves the
directory shape without pretending to be a trained policy.
