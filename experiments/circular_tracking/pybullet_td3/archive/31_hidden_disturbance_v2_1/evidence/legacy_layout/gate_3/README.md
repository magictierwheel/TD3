# Protocol v2.1 — Gate 3

This marker preserves the local Gate 3 layout while the public snapshot keeps
only compact evidence. Large training artifacts remain local and are excluded
by the root layout policy.
