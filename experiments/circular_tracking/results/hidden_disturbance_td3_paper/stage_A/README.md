# Stage A

The local Stage A directory contains training and validation outputs. The
public snapshot keeps compact summaries and manifests only; replay buffers,
checkpoints, caches, videos, and temporary logs remain local. This file marks
the directory on GitHub and does not represent a runnable trained policy.
