# Protocol v2.1 — Gate 3 training

The local tree contains per-controller and per-seed training directories here.
Only compact review evidence is published; large generated training artifacts
remain local under `.gitignore`. This README is a layout marker, not a model.
