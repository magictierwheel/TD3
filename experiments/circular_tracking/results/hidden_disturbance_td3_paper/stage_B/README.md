# Stage B

The local Stage B directory contains multi-seed training outputs. The public
snapshot keeps compact summaries and manifests only; large or disposable
artifacts remain local under `.gitignore`. This file is a layout marker, not a
model or checkpoint.
