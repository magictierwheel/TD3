# Protocol v2

This directory mirrors the local protocol-v2 result layout. GitHub retains the
small gate manifests and metadata needed for review. Local checkpoints, replay
buffers, caches, and temporary logs are intentionally excluded; this README is
only a layout marker and is not a runnable artifact.
